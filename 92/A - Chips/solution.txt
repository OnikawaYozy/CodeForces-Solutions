def solve(n, m):
    sum_one_round = n * (n + 1) // 2
    m %= sum_one_round
    # Phát cho từng con ở vòng cuối cùng
    for i in range(1, n + 1):
        if m >= i:
            m -= i
        else:
            break
            
    return m
n, m = map(int, input().split())
print(solve(n, m))